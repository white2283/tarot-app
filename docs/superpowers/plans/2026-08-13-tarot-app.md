# 塔罗牌 Web 应用实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建塔罗牌占卜全栈 Web 应用(牌阵选择/抽牌/翻牌/混合解析/历史记录),公网部署到 Linux 云服务器,第二阶段加入神谕卡模块。

**Architecture:** 前端 React SPA(Vite 构建)+ Node/Fastify 轻后端(静态伺服 + AI 代理 + SQLite 历史)。核心逻辑(抽牌/解读引擎)为纯 TypeScript 模块,前后端共享牌库与牌阵数据。设计文档:`docs/superpowers/specs/2026-08-13-tarot-app-design.md`。

**Tech Stack:** React 19 + TypeScript + Vite + Tailwind CSS v3 + Framer Motion;Node.js(>=20.19)+ Fastify + better-sqlite3;Vitest;tsx;pm2(部署)。

## Global Constraints

- 运行环境:开发机 Windows(Git Bash),部署机 Linux;Node.js >= 20.19(Vite 8 的 rolldown binding 硬性要求;用全局 fetch,不引 axios)
- 端口:Vite 开发服 `7100`(vite.config.ts 里 `server.port = 7100`,且必须支持 `--port/--host` CLI 参数覆盖);后端 `8787`;开发期 Vite 代理 `/api` → `http://localhost:8787`
- AI 接口:OpenAI 兼容 `POST {AI_BASE_URL}/chat/completions`,Key 仅存服务器 `.env`,绝不下发前端;超时 10 秒;每访客每天限 20 次
- 视觉规范:深紫靛渐变底 `#2b1a4d → #0e0720`,金线主色 `#d4af37`,高光 `#e9cf7a`;牌面 viewBox `240×420`;风格基准 `D:\KimiData\kimi\workspace\tarot-preview\preview.png`
- 字体栈:`"Noto Serif SC", "SimSun", serif`(不依赖网络字体)
- 文案:78 张牌义全部中文改写润色,不照抄参考资料原文;神谕卡无逆位
- 提交:每个任务结束必须 `git commit`,commit message 用中文 conventional 格式
- 不实现:账号系统、支付、AI 生成插画、占星/色彩对应系统功能化

## 文件结构总览

```
tarot-app/
  package.json  tsconfig.json  vite.config.ts  vitest.config.ts
  tailwind.config.js  postcss.config.js  .env.example  .gitignore(已有)
  README.md  ecosystem.config.js  deploy.sh
  shared/
    core/types.ts  deck.ts  interpret.ts  spreads.ts  symbols.ts
    core/__tests__/deck.test.ts  spreads.test.ts  interpret.test.ts  cards.test.ts
    data/cards.tarot.json  cards.moonology.json
  client/
    index.html
    src/main.tsx  App.tsx  index.css
    src/components/CardFace.tsx  CardBack.tsx  SpreadPicker.tsx  FanDeck.tsx
                  FlipCard.tsx  InterpretationView.tsx  PasscodeGate.tsx
    src/pages/HomePage.tsx  DrawPage.tsx  RevealPage.tsx  ReadingPage.tsx  HistoryPage.tsx
    src/state/session.ts
    src/api/client.ts
  server/
    src/index.ts  app.ts  db.ts  auth.ts  interpret.ts  history.ts  ai.ts
    src/__tests__/server.test.ts
```

---

### Task 1: 项目脚手架与开发环境

**Files:**
- Create: `package.json`, `tsconfig.json`, `vite.config.ts`, `vitest.config.ts`, `tailwind.config.js`, `postcss.config.js`, `client/index.html`, `client/src/main.tsx`, `client/src/index.css`, `client/src/App.tsx`, `server/src/index.ts`

**Interfaces:**
- Produces: `npm run dev`(同时起 client:7100 + server:8787)、`npm test`、`npm run build`、`npm start`;后端 `GET /api/health` → `{ "ok": true }`

- [ ] **Step 1: 写根 package.json**

```json
{
  "name": "tarot-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n client,server \"npm:dev:client\" \"npm:dev:server\"",
    "dev:client": "vite",
    "dev:server": "tsx watch server/src/index.ts",
    "build": "vite build",
    "start": "tsx server/src/index.ts",
    "test": "vitest run"
  }
}
```

- [ ] **Step 2: 安装依赖**

Run:
```bash
cd "D:\KimiData\kimi\workspace\tarot-app"
npm install react react-dom react-router-dom framer-motion fastify better-sqlite3
npm install -D typescript vite @vitejs/plugin-react vitest tsx concurrently tailwindcss@3 postcss autoprefixer @types/react @types/react-dom @types/node
```
Expected: 无 ERR;`node_modules/` 生成

- [ ] **Step 3: 写配置文件**

`tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022", "module": "ESNext", "moduleResolution": "bundler",
    "strict": true, "jsx": "react-jsx", "esModuleInterop": true,
    "skipLibCheck": true, "resolveJsonModule": true, "types": ["node"]
  },
  "include": ["shared", "client/src", "server/src"]
}
```

`vite.config.ts`:
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
export default defineConfig({
  root: "client",
  plugins: [react()],
  build: { outDir: "../dist", emptyOutDir: true },
  server: {
    port: 7100,
    proxy: { "/api": "http://localhost:8787" }
  }
});
```

`vitest.config.ts`:
```ts
import { defineConfig } from "vitest/config";
export default defineConfig({
  test: { environment: "node", include: ["shared/**/*.test.ts", "server/**/*.test.ts"] }
});
```

`tailwind.config.js`:
```js
/** @type {import('tailwindcss').Config} */
export default {
  content: ["./client/index.html", "./client/src/**/*.{ts,tsx}"],
  theme: { extend: { colors: { gold: "#d4af37", goldlt: "#e9cf7a", ink: "#0e0720" } } },
  plugins: []
};
```

`postcss.config.js`:
```js
export default { plugins: { tailwindcss: {}, autoprefixer: {} } };
```

- [ ] **Step 4: 最小可运行页面与健康检查**

`client/index.html`:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>星轨塔罗</title>
</head>
<body>
  <div id="root"></div>
  <script type="module" src="/src/main.tsx"></script>
</body>
</html>
```

`client/src/index.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

body {
  font-family: "Noto Serif SC", "SimSun", serif;
  background: #0b0616;
  color: #cbb26a;
}
```

`client/src/main.tsx`:
```tsx
import React from "react";
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";
createRoot(document.getElementById("root")!).render(<App />);
```

`client/src/App.tsx`:
```tsx
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <h1 className="text-2xl tracking-[0.5em] text-gold">星 轨 塔 罗</h1>
    </div>
  );
}
```

`server/src/index.ts`:
```ts
import Fastify from "fastify";

const app = Fastify();
app.get("/api/health", async () => ({ ok: true }));

const port = Number(process.env.PORT ?? 8787);
app.listen({ port, host: "0.0.0.0" }).then(() => {
  console.log(`server listening on ${port}`);
});
```

- [ ] **Step 5: 验证**

Run:
```bash
npm run dev:server &
sleep 3 && curl -s http://localhost:8787/api/health
```
Expected: `{"ok":true}`;然后 `kill %1`
Run: `npm run dev:client` 能启动在 7100(手动确认后 Ctrl+C)

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "chore: 项目脚手架(Vite+React+TS+Tailwind / Fastify / Vitest)"
```

---

### Task 2: 核心类型与抽牌引擎

**Files:**
- Create: `shared/core/types.ts`, `shared/core/deck.ts`
- Test: `shared/core/__tests__/deck.test.ts`

**Interfaces:**
- Produces(后续所有任务依赖):
  - `CardData { id: string; name: string; nameEn: string; arcana: "major"|"minor"|"oracle"; number: number; suit: "wands"|"cups"|"swords"|"pentacles"|null; keywords: string[]; upright: string; reversed: string|null; domains: { general: string; love?: string }; symbols: { type: string; x: number; y: number }[]; palette: { bg: string; fg: string } }`
  - `DeckData { id: string; name: string; type: "tarot"|"oracle"; cards: CardData[] }`
  - `DrawnCard { card: CardData; reversed: boolean }`
  - `SpreadPosition { name: string; meaning: string }`
  - `Spread { id: string; name: string; description: string; cardCount: number; positions: SpreadPosition[] }`
  - `createDeck(data: DeckData): CardData[]`
  - `shuffle<T>(arr: T[], rng?: () => number): T[]`
  - `draw(deck: CardData[], count: number, rng?: () => number): DrawnCard[]`

- [ ] **Step 1: 写失败测试**

`shared/core/__tests__/deck.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { createDeck, shuffle, draw } from "../deck";
import type { DeckData, CardData } from "../types";

function mulberry32(seed: number) {
  let a = seed;
  return () => {
    a |= 0; a = (a + 0x6d2b79f5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function fakeCard(id: string, arcana: CardData["arcana"] = "minor"): CardData {
  return {
    id, name: id, nameEn: id, arcana, number: 1, suit: "wands",
    keywords: ["a", "b", "c"], upright: "正", reversed: "逆",
    domains: { general: "通用" }, symbols: [], palette: { bg: "#000", fg: "#fff" }
  };
}

const tarotData: DeckData = {
  id: "tarot", name: "韦特塔罗", type: "tarot",
  cards: Array.from({ length: 78 }, (_, i) => fakeCard(`c${i}`))
};
const oracleData: DeckData = {
  id: "moon", name: "月相", type: "oracle",
  cards: Array.from({ length: 44 }, (_, i) => fakeCard(`o${i}`, "oracle"))
};

describe("deck 引擎", () => {
  it("createDeck 返回全部牌", () => {
    expect(createDeck(tarotData)).toHaveLength(78);
  });
  it("shuffle 不增不减成员", () => {
    const deck = createDeck(tarotData);
    const s = shuffle(deck, mulberry32(42));
    expect(s).toHaveLength(78);
    expect(new Set(s.map(c => c.id))).toEqual(new Set(deck.map(c => c.id)));
  });
  it("draw 抽出的牌不重复且数量正确", () => {
    const drawn = draw(createDeck(tarotData), 10, mulberry32(7));
    expect(drawn).toHaveLength(10);
    expect(new Set(drawn.map(d => d.card.id)).size).toBe(10);
  });
  it("塔罗牌正逆位各约 50%", () => {
    const rng = mulberry32(99);
    const n = 10000;
    let rev = 0;
    for (let i = 0; i < n; i++) if (draw(createDeck(tarotData), 1, rng)[0].reversed) rev++;
    expect(rev / n).toBeGreaterThan(0.46);
    expect(rev / n).toBeLessThan(0.54);
  });
  it("神谕卡不产生逆位", () => {
    const drawn = draw(createDeck(oracleData), 5, mulberry32(1));
    expect(drawn.every(d => d.reversed === false)).toBe(true);
  });
  it("抽牌数量超过牌堆时报错", () => {
    expect(() => draw(createDeck(oracleData), 99)).toThrow();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run shared/core/__tests__/deck.test.ts`
Expected: FAIL(找不到模块 `../deck`)

- [ ] **Step 3: 实现 types.ts 与 deck.ts**

`shared/core/types.ts`:
```ts
export type Arcana = "major" | "minor" | "oracle";
export type Suit = "wands" | "cups" | "swords" | "pentacles";

export interface SymbolSpec { type: string; x: number; y: number; }

export interface CardData {
  id: string;
  name: string;
  nameEn: string;
  arcana: Arcana;
  number: number;
  suit: Suit | null;
  keywords: string[];
  upright: string;
  reversed: string | null;
  domains: { general: string; love?: string };
  symbols: SymbolSpec[];
  palette: { bg: string; fg: string };
}

export interface DeckData {
  id: string;
  name: string;
  type: "tarot" | "oracle";
  cards: CardData[];
}

export interface DrawnCard { card: CardData; reversed: boolean; }

export interface SpreadPosition { name: string; meaning: string; }

export interface Spread {
  id: string;
  name: string;
  description: string;
  cardCount: number;
  positions: SpreadPosition[];
}

export type Domain = "general" | "love";

export interface InterpretInput {
  question: string;
  domain: Domain;
  spread: Spread;
  drawn: DrawnCard[];
}

export interface PositionReading {
  position: SpreadPosition;
  drawn: DrawnCard;
  text: string;
}

export interface Interpretation {
  summary: string;
  positions: PositionReading[];
  source: "template" | "ai";
}
```

`shared/core/deck.ts`:
```ts
import type { CardData, DeckData, DrawnCard } from "./types";

export function createDeck(data: DeckData): CardData[] {
  return [...data.cards];
}

export function shuffle<T>(arr: T[], rng: () => number = Math.random): T[] {
  const a = [...arr];
  for (let i = a.length - 1; i > 0; i--) {
    const j = Math.floor(rng() * (i + 1));
    [a[i], a[j]] = [a[j], a[i]];
  }
  return a;
}

export function draw(deck: CardData[], count: number, rng: () => number = Math.random): DrawnCard[] {
  if (count > deck.length) throw new Error(`抽 ${count} 张但牌堆只有 ${deck.length} 张`);
  return shuffle(deck, rng).slice(0, count).map(card => ({
    card,
    reversed: card.arcana === "oracle" ? false : rng() < 0.5
  }));
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run shared/core/__tests__/deck.test.ts`
Expected: PASS 6/6

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(core): 抽牌引擎——洗牌/不重复抽取/正逆位(神谕卡无逆位)"
```

---

### Task 3: 牌阵数据(7 个完整定义)

**Files:**
- Create: `shared/core/spreads.ts`
- Test: `shared/core/__tests__/spreads.test.ts`

**Interfaces:**
- Consumes: `Spread`(Task 2)
- Produces: `SPREADS: Spread[]`、`getSpread(id: string): Spread | undefined`(首页/抽牌页/解析页都从这里取牌阵)

- [ ] **Step 1: 写失败测试**

`shared/core/__tests__/spreads.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { SPREADS, getSpread } from "../spreads";

describe("牌阵数据", () => {
  it("共 7 个牌阵", () => {
    expect(SPREADS).toHaveLength(7);
  });
  it("id 全局唯一", () => {
    expect(new Set(SPREADS.map(s => s.id)).size).toBe(7);
  });
  it("每个牌阵 cardCount 与 positions 长度一致", () => {
    for (const s of SPREADS) expect(s.cardCount).toBe(s.positions.length);
  });
  it("getSpread 按 id 查找", () => {
    expect(getSpread("celtic-cross")?.cardCount).toBe(10);
    expect(getSpread("nonexistent")).toBeUndefined();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run shared/core/__tests__/spreads.test.ts`
Expected: FAIL(找不到模块 `../spreads`)

- [ ] **Step 3: 实现 spreads.ts(位置定义取自《牌阵合集》原文,见 spec 4.2)**

`shared/core/spreads.ts`:
```ts
import type { Spread } from "./types";

export const SPREADS: Spread[] = [
  {
    id: "daily", name: "每日一抽", description: "一张牌,一个今日指引", cardCount: 1,
    positions: [{ name: "今日指引", meaning: "今天值得关注与践行的主题" }]
  },
  {
    id: "time-flow", name: "时间之流", description: "过去、现在与未来的流动", cardCount: 3,
    positions: [
      { name: "过去", meaning: "影响现状的过往经验与因素" },
      { name: "现在", meaning: "问题目前的处境与状态" },
      { name: "未来", meaning: "趋势与可能的结果" }
    ]
  },
  {
    id: "body-mind-spirit", name: "身心灵", description: "三个层面审视当下的自己", cardCount: 3,
    positions: [
      { name: "身", meaning: "身体与物质层面的状态" },
      { name: "心", meaning: "心理与情绪层面的状态" },
      { name: "灵", meaning: "精神与灵性层面的状态" }
    ]
  },
  {
    id: "diamond", name: "钻石", description: "现状的双重视角,适合是非题", cardCount: 4,
    positions: [
      { name: "过去", meaning: "问题过去的状况" },
      { name: "现状之一", meaning: "目前情形的一面(与另一面综合解释)" },
      { name: "现状之二", meaning: "目前情形的另一面(与另一面综合解释)" },
      { name: "结果", meaning: "问题的最后结果" }
    ]
  },
  {
    id: "choice", name: "二择一", description: "两难抉择的两条路径比较", cardCount: 5,
    positions: [
      { name: "求问者现况", meaning: "你目前的状态与处境" },
      { name: "选择甲的现况", meaning: "选项 A 当前的基础与条件" },
      { name: "选择乙的现况", meaning: "选项 B 当前的基础与条件" },
      { name: "选择甲的未来", meaning: "若选 A,未来的发展走向" },
      { name: "选择乙的未来", meaning: "若选 B,未来的发展走向" }
    ]
  },
  {
    id: "grand-cross", name: "大十字", description: "简明呈现阻碍、帮助与结果", cardCount: 5,
    positions: [
      { name: "外界影响·一", meaning: "影响问题发展的外部人或事(可为阻碍或帮助)" },
      { name: "外界影响·二", meaning: "影响问题发展的外部人或事(可为阻碍或帮助)" },
      { name: "问题根源", meaning: "问题发生的原因" },
      { name: "解决方式", meaning: "对问题最主要的解决途径" },
      { name: "最终结果", meaning: "问题的最终结果" }
    ]
  },
  {
    id: "celtic-cross", name: "凯尔特十字", description: "经典全景牌阵,深入复杂问题", cardCount: 10,
    positions: [
      { name: "现状", meaning: "问题的核心处境" },
      { name: "横阻", meaning: "横亘眼前的阻碍或助力" },
      { name: "根基", meaning: "问题背后的深层基础" },
      { name: "过去的影响", meaning: "正在消退的过往因素" },
      { name: "目标", meaning: "显意识中的目标与期待" },
      { name: "未来走向", meaning: "即将展开的趋势" },
      { name: "自我认知", meaning: "你对自己与问题的看法" },
      { name: "环境与他人", meaning: "周围环境和他人对你的影响" },
      { name: "希望与恐惧", meaning: "内心深处的期待与担忧" },
      { name: "最终结果", meaning: "各因素汇聚的最终走向" }
    ]
  }
];

export function getSpread(id: string): Spread | undefined {
  return SPREADS.find(s => s.id === id);
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run shared/core/__tests__/spreads.test.ts`
Expected: PASS 4/4

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(core): 7 个内置牌阵定义(依据《牌阵合集》经典定义)"
```

---

### Task 4: 牌库 schema、符号枚举、校验测试与 22 大牌数据

**Files:**
- Create: `shared/core/symbols.ts`, `shared/data/cards.tarot.json`
- Test: `shared/core/__tests__/cards.test.ts`

**Interfaces:**
- Consumes: `CardData`、`DeckData`(Task 2)
- Produces: `SYMBOL_TYPES: readonly string[]`(CardFace 渲染器与数据校验共用);`cards.tarot.json` 符合 `DeckData`(本任务先含 22 大牌,Task 5 补 56 小牌后满足 78 校验)

**数据编写规则(执行者必须遵守):**
1. 内容依据:正/逆位与牌面意象参考 `D:\KimiData\kimi\workspace\tarot-references\` 下的韦特说明书、逆位精解、穆夏说明书;**全部改写润色,不照抄**
2. 每张牌:`keywords` 3–5 个;`upright`/`reversed` 各 60–120 字;`domains.general` 60–120 字,`domains.love` 可选(大牌必写);`symbols` 2–5 个元素,类型必须在 `SYMBOL_TYPES` 内,`x`/`y` 为 0–100 的相对坐标
3. `id` 规则:大牌 `major-00`…`major-21`;小牌 `{suit}-01`…`{suit}-10`、`{suit}-page`、`{suit}-knight`、`{suit}-queen`、`{suit}-king`;宫廷牌 `number` 依次为 page=11、knight=12、queen=13、king=14(牌面顶部罗马数字由此生成)
4. 本任务的校验测试**先只断言 22 张大牌齐全**,Task 5 完成后改为断言 78 张

- [ ] **Step 1: 写符号枚举**

`shared/core/symbols.ts`:
```ts
export const SYMBOL_TYPES = [
  "sun", "moon", "sparkle", "star", "cliff", "dog", "rose", "figure",
  "tower", "lightning", "waves", "chalice", "vine", "grapes",
  "sword", "wand", "pentacle", "crown", "throne", "chariot", "wheel",
  "angel", "lion", "pillar", "veil", "scroll", "lantern", "mountain",
  "river", "tree", "snake", "eagle", "bull", "child", "boat", "key",
  "heart", "cloud", "hand", "crown-of-thorns", "skull", "butterfly"
] as const;
export type SymbolType = typeof SYMBOL_TYPES[number];
```

- [ ] **Step 2: 写失败测试**

`shared/core/__tests__/cards.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { SYMBOL_TYPES } from "../symbols";
import tarot from "../../data/cards.tarot.json";

const symbolSet = new Set<string>(SYMBOL_TYPES);

describe("塔罗牌库数据", () => {
  it("大牌 22 张齐全(Task 5 后扩展为 78 张)", () => {
    const majors = tarot.cards.filter(c => c.arcana === "major");
    expect(majors).toHaveLength(22);
  });
  it("id 全局唯一", () => {
    const ids = tarot.cards.map(c => c.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
  it("每张牌字段完整且合法", () => {
    for (const c of tarot.cards) {
      expect(c.name.length).toBeGreaterThan(0);
      expect(c.keywords.length).toBeGreaterThanOrEqual(3);
      expect(c.keywords.length).toBeLessThanOrEqual(5);
      expect(c.upright.length).toBeGreaterThanOrEqual(30);
      expect(c.domains.general.length).toBeGreaterThanOrEqual(30);
      expect(c.palette.bg).toMatch(/^#[0-9a-f]{6}$/i);
      expect(c.palette.fg).toMatch(/^#[0-9a-f]{6}$/i);
      for (const s of c.symbols) {
        expect(symbolSet.has(s.type)).toBe(true);
        expect(s.x).toBeGreaterThanOrEqual(0);
        expect(s.x).toBeLessThanOrEqual(100);
        expect(s.y).toBeGreaterThanOrEqual(0);
        expect(s.y).toBeLessThanOrEqual(100);
      }
      if (c.arcana === "oracle") expect(c.reversed).toBeNull();
    }
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `npx vitest run shared/core/__tests__/cards.test.ts`
Expected: FAIL(`cards.tarot.json` 不存在)

- [ ] **Step 4: 编写 22 大牌数据**

创建 `shared/data/cards.tarot.json`,结构 `{"id":"rws","name":"韦特塔罗","type":"tarot","cards":[...]}`。三张示范(完整内容,执行者按此水准与规则写全 22 张):

```json
{
  "id": "major-00", "name": "愚人", "nameEn": "The Fool", "arcana": "major", "number": 0, "suit": null,
  "keywords": ["开始", "自由", "冒险", "纯真"],
  "upright": "一张关于全新开始的牌。你正站在一段未知旅程的起点,前路没有地图,但脚下就是答案。放下过多的算计与顾虑,带着赤子之心迈出第一步,世界会接住勇敢的人。",
  "reversed": "逆位的愚人提醒你:此刻的冒险可能欠考虑,或者你正因恐惧而裹足不前。检查自己是冲动行事还是过度谨慎,在跃出悬崖之前,先看一眼脚下。",
  "domains": {
    "general": "生活正为你打开一扇新的门,保持开放与好奇,允许自己犯错,旅程本身就是收获。",
    "love": "感情中迎来新的可能:一段不设防的相遇,或旧关系里的新篇章。别用过去的经验预判眼前人,真心比技巧重要。"
  },
  "symbols": [
    { "type": "sun", "x": 72, "y": 24 }, { "type": "figure", "x": 49, "y": 52 },
    { "type": "rose", "x": 38, "y": 45 }, { "type": "dog", "x": 28, "y": 68 },
    { "type": "cliff", "x": 38, "y": 74 }
  ],
  "palette": { "bg": "#2b1a4d", "fg": "#d4af37" }
}
```

```json
{
  "id": "major-16", "name": "高塔", "nameEn": "The Tower", "arcana": "major", "number": 16, "suit": null,
  "keywords": ["骤变", "崩塌", "觉醒", "释放"],
  "upright": "旧有的结构正在崩塌——那些你以为坚固、实则早已腐朽的部分。骤变带来震动,但也劈开了虚假的安稳。当塔倒下,你终于能看清什么才是真实的地基。",
  "reversed": "逆位的高塔暗示你正死死抓着摇摇欲坠的东西不放,或一场变革在内部悄然酝酿。与其等待闪电,不如主动拆掉危墙,把剧变变成渐进。",
  "domains": {
    "general": "接受改变已至的事实,把精力从'挽回旧局'转向'重建新局',危机会过去,留下的是更结实的自己。",
    "love": "关系中长期回避的问题可能爆发。若感情建立在幻想上,坍塌是解脱;若地基尚好,这场震动反而清除误解。"
  },
  "symbols": [
    { "type": "lightning", "x": 30, "y": 22 }, { "type": "tower", "x": 50, "y": 52 },
    { "type": "figure", "x": 26, "y": 55 }, { "type": "figure", "x": 74, "y": 60 },
    { "type": "waves", "x": 50, "y": 82 }
  ],
  "palette": { "bg": "#241238", "fg": "#d4af37" }
}
```

(大牌余下 19 张:魔术师、女祭司、女皇、皇帝、教皇、恋人、战车、力量、隐士、命运之轮、正义、倒吊人、死神、节制、恶魔、星星、月亮、太阳、审判、世界——按上述字段、字数与规则逐一编写。)

- [ ] **Step 5: 运行确认通过**

Run: `npx vitest run shared/core/__tests__/cards.test.ts`
Expected: PASS 3/3

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(data): 22 张大阿尔卡那牌义与符号场景数据 + 数据校验"
```

---

### Task 5: 56 张小阿尔卡那数据(分 4 批次)

**Files:**
- Modify: `shared/data/cards.tarot.json`(追加小牌)
- Modify: `shared/core/__tests__/cards.test.ts`(大牌断言改为 78 张总断言)

**Interfaces:**
- Consumes: Task 4 的数据规则与 `SYMBOL_TYPES`
- Produces: 完整 78 张塔罗牌库(后续 UI/引擎直接用)

- [ ] **Step 1: 权杖(wands)14 张**

按 Task 4 规则编写 `wands-01`(王牌)至 `wands-10` 与 `wands-page/knight/queen/king`。权杖=火元素,主题:行动、热情、事业;符号优先用注册表内的 `wand`、`sparkle`、`lion`、`sun`、`mountain`。`domains.love` 对宫廷牌必写,数字牌可选。

- [ ] **Step 2: 运行校验**

Run: `npx vitest run shared/core/__tests__/cards.test.ts`
Expected: PASS(字段合法性覆盖已写牌)

- [ ] **Step 3: 圣杯(cups)14 张**

圣杯=水元素,主题:情感、关系、直觉;符号优先 `chalice`、`waves`、`heart`、`river`、`cloud`。写完后运行 `npx vitest run shared/core/__tests__/cards.test.ts`,Expected: PASS

- [ ] **Step 4: 宝剑(swords)14 张**

宝剑=风元素,主题:思维、冲突、真相;符号优先 `sword`、`cloud`、`eagle`、`crown-of-thorns`。写完后运行同一校验,Expected: PASS

- [ ] **Step 5: 星币(pentacles)14 张**

星币=土元素,主题:物质、工作、健康;符号优先 `pentacle`、`tree`、`mountain`、`key`。写完后运行同一校验,Expected: PASS

- [ ] **Step 6: 把测试升级为 78 张总断言**

修改 `cards.test.ts` 第一条用例:
```ts
it("牌库 78 张齐全(22 大牌 + 56 小牌)", () => {
  expect(tarot.cards).toHaveLength(78);
  expect(tarot.cards.filter(c => c.arcana === "major")).toHaveLength(22);
  expect(tarot.cards.filter(c => c.arcana === "minor")).toHaveLength(56);
});
```
Run: `npx vitest run shared/core/__tests__/cards.test.ts`
Expected: PASS 3/3

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(data): 56 张小阿尔卡那数据,78 张牌库完整"
```

---

### Task 6: 模板解读引擎(含领域匹配与 AI 回退)

**Files:**
- Create: `shared/core/interpret.ts`
- Test: `shared/core/__tests__/interpret.test.ts`

**Interfaces:**
- Consumes: `InterpretInput`、`Interpretation`、`Domain`、`DrawnCard`、`Spread`(Task 2)
- Produces:
  - `templateInterpret(input: InterpretInput): Interpretation`
  - `type AiCall = (input: InterpretInput) => Promise<string>`
  - `interpretWithFallback(input: InterpretInput, aiCall?: AiCall): Promise<Interpretation>`——AI 成功时 `source: "ai"` 且 `summary` 为 AI 文本(逐位解读仍保留模板结果);任何失败/未传 aiCall 时静默回退 `source: "template"`

**组合规则(实现者严格遵守):**
- 逐位文案:逆位 → 用 `card.reversed`;正位 → 优先 `card.domains[input.domain]`,缺省回退 `domains.general`
- 逐位格式:`${card.name}·${正位|逆位} —— ${position.meaning}。${牌义文案}`
- 整体叙事:`关于「${question || "你心中的问题"}」,${spread.name}给出的线索是:${每张牌 keywords[0] 顿号连接}。` + 若逆位过半追加"逆位居多,提示当下宜内观与调整,暂缓重大行动。",否则追加"牌面能量总体顺畅,可以顺着指引的方向稳步前行。"

- [ ] **Step 1: 写失败测试**

`shared/core/__tests__/interpret.test.ts`:
```ts
import { describe, it, expect } from "vitest";
import { templateInterpret, interpretWithFallback } from "../interpret";
import type { CardData, InterpretInput, Spread } from "../types";

const cardA: CardData = {
  id: "a", name: "愚人", nameEn: "The Fool", arcana: "major", number: 0, suit: null,
  keywords: ["开始", "自由", "冒险"], upright: "正位通用文案A", reversed: "逆位文案A",
  domains: { general: "通用领域文案A", love: "爱情领域文案A" },
  symbols: [], palette: { bg: "#000", fg: "#fff" }
};
const cardB: CardData = { ...cardA, id: "b", name: "高塔", keywords: ["骤变"], domains: { general: "通用领域文案B" } };

const spread: Spread = {
  id: "s", name: "时间之流", description: "", cardCount: 2,
  positions: [
    { name: "过去", meaning: "过往因素" },
    { name: "现在", meaning: "当前状态" }
  ]
};

function input(domain: "general" | "love", reversedB = false): InterpretInput {
  return {
    question: "我该换工作吗", domain, spread,
    drawn: [{ card: cardA, reversed: false }, { card: cardB, reversed: reversedB }]
  };
}

describe("模板解读引擎", () => {
  it("正位且领域命中时用领域文案", () => {
    const r = templateInterpret(input("love"));
    expect(r.positions[0].text).toContain("爱情领域文案A");
    expect(r.positions[0].text).toContain("愚人·正位");
    expect(r.source).toBe("template");
  });
  it("领域文案缺失时回退 general", () => {
    const r = templateInterpret(input("love"));
    expect(r.positions[1].text).toContain("通用领域文案B");
  });
  it("逆位时用逆位文案", () => {
    const r = templateInterpret(input("general", true));
    expect(r.positions[1].text).toContain("逆位文案A");
    expect(r.positions[1].text).toContain("高塔·逆位");
  });
  it("整体叙事含关键词与问题", () => {
    const r = templateInterpret(input("general"));
    expect(r.summary).toContain("我该换工作吗");
    expect(r.summary).toContain("开始");
    expect(r.summary).toContain("骤变");
  });
  it("不传 aiCall 时回退模板", async () => {
    const r = await interpretWithFallback(input("general"));
    expect(r.source).toBe("template");
  });
  it("AI 失败时静默回退模板", async () => {
    const r = await interpretWithFallback(input("general"), async () => { throw new Error("boom"); });
    expect(r.source).toBe("template");
  });
  it("AI 成功时 summary 用 AI 文本,逐位保留模板", async () => {
    const r = await interpretWithFallback(input("general"), async () => "AI 深度解读文本");
    expect(r.source).toBe("ai");
    expect(r.summary).toBe("AI 深度解读文本");
    expect(r.positions[0].text).toContain("愚人·正位");
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run shared/core/__tests__/interpret.test.ts`
Expected: FAIL(找不到模块 `../interpret`)

- [ ] **Step 3: 实现 interpret.ts**

`shared/core/interpret.ts`:
```ts
import type { InterpretInput, Interpretation, PositionReading } from "./types";

export function templateInterpret(input: InterpretInput): Interpretation {
  const positions: PositionReading[] = input.drawn.map((d, i) => {
    const position = input.spread.positions[i];
    const orientation = d.reversed ? "逆位" : "正位";
    const copy = d.reversed
      ? (d.card.reversed ?? d.card.domains.general)
      : (input.domain !== "general" && d.card.domains[input.domain]
          ? d.card.domains[input.domain]!
          : d.card.domains.general);
    return { position, drawn: d, text: `${d.card.name}·${orientation} —— ${position.meaning}。${copy}` };
  });
  const kws = input.drawn.map(d => d.card.keywords[0]);
  const reversedCount = input.drawn.filter(d => d.reversed).length;
  const tail = reversedCount > input.drawn.length / 2
    ? "逆位居多,提示当下宜内观与调整,暂缓重大行动。"
    : "牌面能量总体顺畅,可以顺着指引的方向稳步前行。";
  const summary = `关于「${input.question.trim() || "你心中的问题"}」,${input.spread.name}给出的线索是:${kws.join("、")}。${tail}`;
  return { summary, positions, source: "template" };
}

export type AiCall = (input: InterpretInput) => Promise<string>;

export async function interpretWithFallback(input: InterpretInput, aiCall?: AiCall): Promise<Interpretation> {
  const base = templateInterpret(input);
  if (!aiCall) return base;
  try {
    const text = await aiCall(input);
    return { ...base, summary: text, source: "ai" };
  } catch {
    return base;
  }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run shared/core/__tests__/interpret.test.ts`
Expected: PASS 7/7

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(core): 模板解读引擎——领域匹配/逆位文案/整体叙事/AI 静默回退"
```

---

### Task 7: CardFace 符号渲染组件与牌背

**Files:**
- Create: `client/src/components/CardFace.tsx`, `client/src/components/CardBack.tsx`
- Create(临时验收页,Task 8 接入路由后移除): `client/src/pages/DevCardsPage.tsx`

**Interfaces:**
- Consumes: `CardData`(Task 2)、`SYMBOL_TYPES`(Task 4)
- Produces:
  - `<CardFace card={CardData} reversed?: boolean />`——输出 240×420 viewBox 的 SVG;`reversed` 时整体旋转 180°
  - `<CardBack />`——曼陀罗牌背
  - 约定:symbols 坐标为 0–100 相对值,组件内映射 `x*2.4, y*4.2`

- [ ] **Step 1: 实现 CardFace.tsx(完整符号渲染器注册表)**

```tsx
import type { CardData, SymbolSpec } from "../../../shared/core/types";
import type { ReactNode } from "react";

const GOLD = "#d4af37";
const LT = "#e9cf7a";

function toRoman(n: number): string {
  const t: [number, string][] = [[10, "X"], [9, "IX"], [5, "V"], [4, "IV"], [1, "I"]];
  let r = "";
  for (const [v, s] of t) while (n >= v) { r += s; n -= v; }
  return r || "0";
}

const SPARKLE = "M0 -6 L1.6 -1.6 L6 0 L1.6 1.6 L0 6 L-1.6 1.6 L-6 0 L-1.6 -1.6 Z";

type Renderer = (s: SymbolSpec) => JSX.Element;
const g = (s: SymbolSpec, children: ReactNode) => (
  <g transform={`translate(${s.x * 2.4} ${s.y * 4.2})`}>{children}</g>
);

const RENDERERS: Record<string, Renderer> = {
  sun: s => g(s, <>
    <circle r="20" fill="none" stroke={LT} strokeWidth="2" />
    {Array.from({ length: 12 }, (_, i) => {
      const a = (i * Math.PI) / 6;
      return <line key={i} x1={26 * Math.cos(a)} y1={26 * Math.sin(a)} x2={35 * Math.cos(a)} y2={35 * Math.sin(a)} stroke={LT} strokeWidth="2.4" strokeLinecap="round" />;
    })}
  </>),
  moon: s => g(s, <path d="M4 -11 A12 12 0 1 0 4 11 A9 9 0 1 1 4 -11 Z" fill={GOLD} opacity="0.9" />),
  sparkle: s => g(s, <path d={SPARKLE} fill={LT} opacity="0.85" />),
  star: s => g(s, <path d={SPARKLE} fill={LT} transform="scale(1.8)" opacity="0.9" />),
  cliff: s => g(s, <>
    <path d="M-42 0 H42 L34 18 H-30 L-36 10 Z" fill="none" stroke={GOLD} strokeWidth="2" strokeLinejoin="round" />
    <path d="M-12 0 L-16 18 M14 0 L10 18" stroke={GOLD} strokeWidth="1" opacity="0.4" />
  </>),
  dog: s => g(s, <path d="M-20 10 v-13 l6 -8 h16 l7 8 v10 M-4 -11 l5 -9 l5 5 M-20 -1 l-9 -8 M-14 10 v-8 M3 10 v-8" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" />),
  rose: s => g(s, <>
    <circle r="6" fill="none" stroke={GOLD} strokeWidth="1.2" />
    <circle cy="-1.5" r="3" fill="none" stroke={GOLD} strokeWidth="1" />
  </>),
  figure: s => g(s, <>
    <circle cy="-16" r="7.5" fill="none" stroke={GOLD} strokeWidth="2" />
    <path d="M0 -8.5 V32 M0 32 L-13 60 M0 32 L16 52 M0 0 L-21 -18 M0 0 L23 -14 L33 -36" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />
    <circle cx="36" cy="-43" r="6.5" fill="none" stroke={GOLD} strokeWidth="2" />
  </>),
  tower: s => g(s, <>
    <path d="M-26 76 V-26 H26 V76 M-30 -26 v-16 h12 v9 h10 v-9 h12 v9 h10 v-9 h12 v16" fill="none" stroke={GOLD} strokeWidth="2" strokeLinejoin="round" />
    <circle cy="-40" r="5.5" fill="none" stroke={GOLD} strokeWidth="2" transform="translate(0 26)" />
    <path d="M-7 76 V50 a7 7 0 0 1 14 0 V76" fill="none" stroke={GOLD} strokeWidth="2" />
  </>),
  lightning: s => g(s, <path d="M-30 -40 L6 6 L-10 10 L16 48" fill="none" stroke={LT} strokeWidth="3.5" strokeLinejoin="round" />),
  waves: s => g(s, <>
    <path d="M-70 0 q11 -9 22 0 t22 0 t22 0 t22 0" fill="none" stroke={GOLD} strokeWidth="1" opacity="0.5" />
    <path d="M-60 16 q11 -9 22 0 t22 0 t22 0" fill="none" stroke={GOLD} strokeWidth="1" opacity="0.5" />
  </>),
  chalice: s => g(s, <>
    <path d="M-17 -22 h34 c0 15 -8.5 23 -17 23 c-8.5 0 -17 -8 -17 -22 z" fill="none" stroke={GOLD} strokeWidth="2" />
    <path d="M0 1 v15 M-11 16 h22" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />
  </>),
  vine: s => g(s, <path d="M-48 -6 Q0 32 48 -6" fill="none" stroke={GOLD} strokeWidth="1.2" />),
  grapes: s => g(s, <>
    {[[-7, -8], [7, -8], [0, -1], [-7, 6], [7, 6]].map(([x, y], i) => (
      <circle key={i} cx={x} cy={y} r="3.2" fill={GOLD} opacity="0.85" />
    ))}
  </>),
  sword: s => g(s, <path d="M0 -30 V14 M-12 14 H12 M-4 22 H4 M0 14 V22" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />),
  wand: s => g(s, <path d="M-14 24 L14 -24 M14 -24 l4 -4 M10 -20 l6 2" fill="none" stroke={GOLD} strokeWidth="2.4" strokeLinecap="round" />),
  pentacle: s => g(s, <>
    <circle r="16" fill="none" stroke={GOLD} strokeWidth="2" />
    <path d="M0 -10 L9.5 7 H-9.5 Z M0 10 L-9.5 -7 H9.5 Z" fill="none" stroke={GOLD} strokeWidth="1.4" />
  </>),
  crown: s => g(s, <path d="M-16 8 L-16 -6 L-8 2 L0 -10 L8 2 L16 -6 L16 8 Z" fill="none" stroke={GOLD} strokeWidth="2" strokeLinejoin="round" />),
  wheel: s => g(s, <>
    <circle r="18" fill="none" stroke={GOLD} strokeWidth="2" />
    <circle r="6" fill="none" stroke={GOLD} strokeWidth="1.4" />
    {[0, 45, 90, 135, 180, 225, 270, 315].map(a => {
      const r = (a * Math.PI) / 180;
      return <line key={a} x1={6 * Math.cos(r)} y1={6 * Math.sin(r)} x2={18 * Math.cos(r)} y2={18 * Math.sin(r)} stroke={GOLD} strokeWidth="1.2" />;
    })}
  </>),
  angel: s => g(s, <>
    <circle cy="-14" r="6" fill="none" stroke={GOLD} strokeWidth="2" />
    <path d="M0 -8 V16 M0 -4 C-14 -16 -24 -14 -28 -4 M0 -4 C14 -16 24 -14 28 -4" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />
  </>),
  lion: s => g(s, <>
    <circle r="12" fill="none" stroke={GOLD} strokeWidth="2" />
    <circle r="19" fill="none" stroke={GOLD} strokeWidth="4" strokeDasharray="0.1 9.84" strokeLinecap="round" />
  </>),
  pillar: s => g(s, <path d="M0 -28 V28 M-10 -28 H10 M-10 28 H10" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />),
  lantern: s => g(s, <>
    <path d="M-8 -10 H8 L10 6 V14 H-10 V6 Z" fill="none" stroke={GOLD} strokeWidth="2" strokeLinejoin="round" />
    <circle cy="4" r="3" fill={LT} opacity="0.9" />
    <path d="M-4 -10 a4 4 0 0 1 8 0" fill="none" stroke={GOLD} strokeWidth="1.4" />
  </>),
  mountain: s => g(s, <path d="M-34 16 L-12 -12 L2 2 L18 -14 L36 16" fill="none" stroke={GOLD} strokeWidth="1.4" opacity="0.6" />),
  river: s => g(s, <path d="M-40 -6 q10 -8 20 0 t20 0 t20 0 M-40 10 q10 -8 20 0 t20 0 t20 0" fill="none" stroke={GOLD} strokeWidth="1" opacity="0.5" />),
  tree: s => g(s, <>
    <path d="M0 22 V-2 M0 6 L-12 -6 M0 6 L12 -8 M0 -2 L-7 -12 M0 -2 L8 -14" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />
    <path d="M-12 22 H12" stroke={GOLD} strokeWidth="1.4" />
  </>),
  heart: s => g(s, <path d="M0 12 C-14 2 -16 -8 -8 -11 C-3 -13 0 -9 0 -6 C0 -9 3 -13 8 -11 C16 -8 14 2 0 12 Z" fill="none" stroke={GOLD} strokeWidth="2" />),
  cloud: s => g(s, <path d="M-18 6 a8 8 0 0 1 4 -15 a10 10 0 0 1 19 3 a7 7 0 0 1 13 5 Z" fill="none" stroke={GOLD} strokeWidth="1.6" />),
  hand: s => g(s, <path d="M-6 16 V-4 M-2 16 V-10 M2 16 V-12 M6 16 V-8 M-10 16 V2 a4 4 0 0 1 4 -6" fill="none" stroke={GOLD} strokeWidth="1.8" strokeLinecap="round" />),
  key: s => g(s, <>
    <circle cx="-10" cy="-6" r="7" fill="none" stroke={GOLD} strokeWidth="2" />
    <path d="M-4 -1 L12 15 M8 11 l5 -2 M11 14 l4 -2" fill="none" stroke={GOLD} strokeWidth="2" strokeLinecap="round" />
  </>)
};

const FALLBACK: Renderer = s => g(s, <path d={SPARKLE} fill={GOLD} opacity="0.7" />);

export default function CardFace({ card, reversed = false }: { card: CardData; reversed?: boolean }) {
  const topLabel = card.arcana === "major" ? toRoman(card.number)
    : card.arcana === "minor" ? toRoman(card.number) : "✦";
  return (
    <svg viewBox="0 0 240 420" className="w-full h-full"
         style={reversed ? { transform: "rotate(180deg)" } : undefined}>
      <defs>
        <radialGradient id={`bg-${card.id}`} cx="50%" cy="32%" r="85%">
          <stop offset="0%" stopColor="#2b1a4d" />
          <stop offset="55%" stopColor="#180d30" />
          <stop offset="100%" stopColor="#0e0720" />
        </radialGradient>
      </defs>
      <rect x="1" y="1" width="238" height="418" rx="16" fill={`url(#bg-${card.id})`} stroke={GOLD} strokeWidth="2" />
      <rect x="9" y="9" width="222" height="402" rx="11" fill="none" stroke={GOLD} strokeWidth="0.6" opacity="0.45" />
      <text x="120" y="46" textAnchor="middle" fill={GOLD} fontSize="15" fontFamily="serif" letterSpacing="2">{topLabel}</text>
      {card.symbols.map((s, i) => {
        const R = RENDERERS[s.type] ?? FALLBACK;
        return <g key={i}>{R(s)}</g>;
      })}
      <text x="120" y="376" textAnchor="middle" fill={LT} fontSize="21" letterSpacing="4"
            fontFamily='"Noto Serif SC","SimSun",serif'>{card.name}</text>
      <text x="120" y="396" textAnchor="middle" fill={GOLD} fontSize="8.5" letterSpacing="3.5"
            fontFamily="serif" opacity="0.8">{card.nameEn.toUpperCase()}</text>
    </svg>
  );
}
```

- [ ] **Step 2: 实现 CardBack.tsx(曼陀罗牌背)**

```tsx
const GOLD = "#d4af37";
const LT = "#e9cf7a";

export default function CardBack() {
  return (
    <svg viewBox="0 0 240 420" className="w-full h-full">
      <defs>
        <radialGradient id="backbg" cx="50%" cy="32%" r="85%">
          <stop offset="0%" stopColor="#2b1a4d" />
          <stop offset="100%" stopColor="#0e0720" />
        </radialGradient>
      </defs>
      <rect x="1" y="1" width="238" height="418" rx="16" fill="url(#backbg)" stroke={GOLD} strokeWidth="2" />
      <rect x="9" y="9" width="222" height="402" rx="11" fill="none" stroke={GOLD} strokeWidth="0.6" opacity="0.45" />
      <circle cx="120" cy="210" r="72" fill="none" stroke={GOLD} strokeWidth="1.6" />
      <circle cx="120" cy="210" r="54" fill="none" stroke={GOLD} strokeWidth="1.6" />
      <rect x="84" y="174" width="72" height="72" fill="none" stroke={GOLD} strokeWidth="1.6" />
      <rect x="84" y="174" width="72" height="72" fill="none" stroke={GOLD} strokeWidth="1.6" transform="rotate(45 120 210)" />
      <circle cx="120" cy="210" r="14" fill="none" stroke={LT} strokeWidth="1.6" />
      <circle cx="120" cy="210" r="4" fill={LT} />
      {Array.from({ length: 36 }, (_, i) => {
        const a = (i * Math.PI * 2) / 36;
        return <line key={i} x1={120 + 80 * Math.cos(a)} y1={210 + 80 * Math.sin(a)}
                     x2={120 + 88 * Math.cos(a)} y2={210 + 88 * Math.sin(a)} stroke={GOLD} strokeWidth="1.4" />;
      })}
      <path d="M124 83 A12 12 0 1 0 124 105 A9 9 0 1 1 124 83 Z" fill={GOLD} opacity="0.9" />
      <circle cx="120" cy="330" r="10" fill="none" stroke={GOLD} strokeWidth="1.6" />
      {Array.from({ length: 12 }, (_, i) => {
        const a = (i * Math.PI * 2) / 12;
        return <line key={i} x1={120 + 14 * Math.cos(a)} y1={330 + 14 * Math.sin(a)}
                     x2={120 + 21 * Math.cos(a)} y2={330 + 21 * Math.sin(a)} stroke={GOLD} strokeWidth="1.6" strokeLinecap="round" />;
      })}
    </svg>
  );
}
```

- [ ] **Step 3: 临时验收页 DevCardsPage.tsx**

```tsx
import CardFace from "../components/CardFace";
import CardBack from "../components/CardBack";
import tarot from "../../../shared/data/cards.tarot.json";

export default function DevCardsPage() {
  const sample = ["major-00", "major-16", "cups-03"]
    .map(id => tarot.cards.find(c => c.id === id)!)
    .filter(Boolean);
  return (
    <div className="min-h-screen flex gap-8 items-center justify-center p-8">
      {sample.map(c => <div key={c.id} className="w-56"><CardFace card={c} /></div>)}
      <div className="w-56"><CardBack /></div>
    </div>
  );
}
```
并临时把 `App.tsx` 改为渲染 `<DevCardsPage />`。

- [ ] **Step 4: 手动验收**

Run: `npm run dev:client`,浏览器打开 `http://localhost:7100`
Expected:愚人、高塔、圣杯三、牌背四张牌与 `D:\KimiData\kimi\workspace\tarot-preview\preview.png` 风格一致(金色线条/深紫底/边框/牌名);视觉确认后 Ctrl+C

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(client): CardFace 符号渲染器注册表(22 种符号)与曼陀罗牌背"
```

---

### Task 8: 会话状态机与视图骨架

**Files:**
- Create: `client/src/state/session.ts`
- Modify: `client/src/App.tsx`(改为状态驱动的视图切换;移除 DevCardsPage)
- Create: `client/src/pages/HomePage.tsx` 等 5 个页面的占位版本(本任务只搭骨架,Task 9/10 填充)

**Interfaces:**
- Produces:
  - `type Phase = "home" | "draw" | "reveal" | "reading" | "history"`
  - `SessionState { phase, deckId: string, spreadId: string|null, question: string, domain: Domain, drawn: DrawnCard[], interpretation: Interpretation|null }`
  - `Action`:`{type:"select-spread", spreadId}` / `{type:"set-question", question, domain}` / `{type:"drawn", drawn}` / `{type:"interpreted", interpretation}` / `{type:"go", phase}` / `{type:"reset"}`
  - `reducer(state, action)`、`initialSession`

- [ ] **Step 1: 实现 session.ts**

```ts
import type { Domain, DrawnCard, Interpretation } from "../../../shared/core/types";

export type Phase = "home" | "draw" | "reveal" | "reading" | "history";

export interface SessionState {
  phase: Phase;
  deckId: string;
  spreadId: string | null;
  question: string;
  domain: Domain;
  drawn: DrawnCard[];
  interpretation: Interpretation | null;
}

export const initialSession: SessionState = {
  phase: "home", deckId: "rws", spreadId: null,
  question: "", domain: "general", drawn: [], interpretation: null
};

export type Action =
  | { type: "select-spread"; spreadId: string }
  | { type: "set-question"; question: string; domain: Domain }
  | { type: "drawn"; drawn: DrawnCard[] }
  | { type: "interpreted"; interpretation: Interpretation }
  | { type: "go"; phase: Phase }
  | { type: "reset" };

export function reducer(state: SessionState, a: Action): SessionState {
  switch (a.type) {
    case "select-spread": return { ...state, spreadId: a.spreadId, phase: "draw" };
    case "set-question": return { ...state, question: a.question, domain: a.domain };
    case "drawn": return { ...state, drawn: a.drawn, phase: "reveal" };
    case "interpreted": return { ...state, interpretation: a.interpretation, phase: "reading" };
    case "go": return { ...state, phase: a.phase };
    case "reset": return { ...initialSession };
  }
}
```

- [ ] **Step 2: 改写 App.tsx(占位页面内联,Task 9/10 再抽为独立文件)**

```tsx
import { useReducer } from "react";
import { initialSession, reducer } from "./state/session";

export default function App() {
  const [s, dispatch] = useReducer(reducer, initialSession);
  return (
    <div className="min-h-screen">
      {s.phase === "home" && <div>首页(Task 9)</div>}
      {s.phase === "draw" && <div>抽牌页(Task 9)</div>}
      {s.phase === "reveal" && <div>翻牌页(Task 10)</div>}
      {s.phase === "reading" && <div>解析页(Task 10)</div>}
      {s.phase === "history" && <div>历史页(Task 13)</div>}
    </div>
  );
}
```

- [ ] **Step 3: 验证**

Run: `npm run dev:client`,确认 `http://localhost:7100` 显示"首页(Task 9)"
Expected: 页面正常渲染,无控制台报错

- [ ] **Step 4: Commit**

```bash
git add -A && git commit -m "feat(client): 会话状态机与五视图骨架"
```

---

### Task 9: 首页与抽牌页(牌阵选择 + 洗牌/扇形铺开/点选)

**Files:**
- Create: `client/src/pages/HomePage.tsx`, `client/src/pages/DrawPage.tsx`, `client/src/components/SpreadPicker.tsx`, `client/src/components/FanDeck.tsx`
- Modify: `client/src/App.tsx`(接入真实页面)

**Interfaces:**
- Consumes: `SPREADS`(Task 3)、`createDeck/draw`(Task 2)、session Action(Task 8)、`cards.tarot.json`(Task 5)、`<CardBack/>`(Task 7)
- Produces: `<FanDeck total={78} pickCount={n} onComplete(drawnIds: number[]) />`——扇形牌背,点满 n 张回调;`<SpreadPicker onSelect(spreadId) />`

- [ ] **Step 1: SpreadPicker.tsx(牌阵卡片列表)**

```tsx
import { SPREADS } from "../../../shared/core/spreads";

export default function SpreadPicker({ onSelect }: { onSelect: (id: string) => void }) {
  return (
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5 max-w-4xl mx-auto">
      {SPREADS.map(s => (
        <button key={s.id} onClick={() => onSelect(s.id)}
          className="border border-gold/40 rounded-xl p-5 text-left hover:border-gold hover:-translate-y-1 transition-all bg-ink/60">
          <div className="text-goldlt text-lg tracking-widest">{s.name}</div>
          <div className="text-sm opacity-60 mt-2">{s.description}</div>
          <div className="text-xs opacity-40 mt-3">{s.cardCount} 张牌</div>
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: HomePage.tsx(标题 + 牌阵选择 + 历史入口)**

```tsx
import SpreadPicker from "../components/SpreadPicker";

export default function HomePage({ onSelect, onHistory }: { onSelect: (id: string) => void; onHistory: () => void }) {
  return (
    <div className="min-h-screen flex flex-col items-center px-6 py-14">
      <h1 className="text-3xl tracking-[0.6em] text-goldlt mb-3">星 轨 塔 罗</h1>
      <p className="text-sm opacity-50 tracking-widest mb-12">静心,默念你的问题,然后选择一个牌阵</p>
      <SpreadPicker onSelect={onSelect} />
      <button onClick={onHistory} className="mt-12 text-sm text-gold/60 hover:text-gold tracking-widest">
        查看历史解读 →
      </button>
    </div>
  );
}
```

- [ ] **Step 3: FanDeck.tsx(洗牌 → 扇形铺开 → 点选)**

```tsx
import { useMemo, useState } from "react";
import { motion } from "framer-motion";
import CardBack from "./CardBack";

interface Props { total: number; pickCount: number; onComplete: (indexes: number[]) => void; }

export default function FanDeck({ total, pickCount, onComplete }: Props) {
  const [picked, setPicked] = useState<number[]>([]);
  const [shuffled, setShuffled] = useState(false);
  // 扇形参数:以底部中心为圆心展开
  const angles = useMemo(() => {
    const spread = 100; // 总角度
    return Array.from({ length: total }, (_, i) => -spread / 2 + (spread * i) / (total - 1));
  }, [total]);

  const pick = (i: number) => {
    if (picked.includes(i)) return;
    const next = [...picked, i];
    setPicked(next);
    if (next.length === pickCount) setTimeout(() => onComplete(next), 400);
  };

  return (
    <div className="relative w-full h-[420px] overflow-hidden">
      {!shuffled && (
        <motion.div className="absolute left-1/2 bottom-0 w-24 h-40 -translate-x-1/2 cursor-pointer"
          animate={{ x: [-30, 30, -15, 15, 0] }} transition={{ duration: 1.2, repeat: 2 }}
          onAnimationComplete={() => setShuffled(true)}>
          <CardBack />
          <div className="text-center text-xs mt-2 opacity-60">洗牌中…</div>
        </motion.div>
      )}
      {shuffled && angles.map((a, i) => (
        <motion.div key={i}
          className="absolute left-1/2 bottom-[-40px] w-16 h-28 origin-bottom cursor-pointer"
          initial={{ rotate: 0, opacity: 0 }}
          animate={{ rotate: a, opacity: 1, y: picked.includes(i) ? -30 : 0 }}
          transition={{ delay: i * 0.008, type: "spring", stiffness: 120 }}
          whileHover={{ y: -24 }}
          onClick={() => pick(i)}>
          <CardBack />
        </motion.div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: DrawPage.tsx(问题输入 + 领域 + 扇形抽牌)**

```tsx
import { useState } from "react";
import FanDeck from "../components/FanDeck";
import { createDeck, draw } from "../../../shared/core/deck";
import { getSpread } from "../../../shared/core/spreads";
import type { DeckData, Domain, DrawnCard } from "../../../shared/core/types";
import tarot from "../../../shared/data/cards.tarot.json";

interface Props {
  spreadId: string;
  onDone: (question: string, domain: Domain, drawn: DrawnCard[]) => void;
}

export default function DrawPage({ spreadId, onDone }: Props) {
  const spread = getSpread(spreadId)!;
  const [question, setQuestion] = useState("");
  const [domain, setDomain] = useState<Domain>("general");
  const [stage, setStage] = useState<"ask" | "draw">("ask");

  if (stage === "ask") {
    return (
      <div className="min-h-screen flex flex-col items-center justify-center px-6 gap-8">
        <div className="text-goldlt tracking-widest text-xl">{spread.name} · {spread.cardCount} 张</div>
        <textarea value={question} onChange={e => setQuestion(e.target.value)}
          placeholder="默念后写下你的问题(也可留白)"
          className="w-full max-w-md h-28 bg-ink/60 border border-gold/40 rounded-xl p-4 text-goldlt placeholder:opacity-40 focus:border-gold outline-none" />
        <div className="flex gap-4">
          {(["general", "love"] as Domain[]).map(d => (
            <button key={d} onClick={() => setDomain(d)}
              className={`px-6 py-2 rounded-full border tracking-widest ${domain === d ? "border-gold text-goldlt" : "border-gold/30 opacity-50"}`}>
              {d === "general" ? "综合" : "爱情"}
            </button>
          ))}
        </div>
        <button onClick={() => setStage("draw")}
          className="px-10 py-3 border border-gold rounded-full text-goldlt tracking-[0.3em] hover:bg-gold/10">
          开始洗牌
        </button>
      </div>
    );
  }
  return (
    <div className="min-h-screen flex flex-col items-center justify-center px-6">
      <p className="text-sm opacity-60 tracking-widest mb-6">凭直觉,依次选出 {spread.cardCount} 张牌</p>
      <FanDeck total={78} pickCount={spread.cardCount}
        onComplete={idxs => {
          // 点选是仪式感的载体;发牌随机性由 draw() 保证(每张物理意义独立随机)
          void idxs;
          const deck = createDeck(tarot as unknown as DeckData);
          const drawn = draw(deck, spread.cardCount);
          onDone(question, domain, drawn);
        }} />
    </div>
  );
}
```

- [ ] **Step 5: App.tsx 接入 Home/Draw**

把 `home` 分支改为 `<HomePage onSelect={id => dispatch({type:"select-spread", spreadId:id})} onHistory={() => dispatch({type:"go", phase:"history"})} />`;`draw` 分支改为 `<DrawPage spreadId={s.spreadId!} onDone={(question, domain, drawn) => { dispatch({type:"set-question", question, domain}); dispatch({type:"drawn", drawn}); }} />`。

- [ ] **Step 6: 手动验收**

Run: `npm run dev:client`;流程:首页选牌阵 → 输入问题选领域 → 洗牌动画 → 扇形 78 张牌背 → 点满张数后进入翻牌占位页
Expected: 动画流畅,点选有上浮反馈,状态正确流转

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat(client): 首页牌阵选择与抽牌页(洗牌/扇形铺开/点选动画)"
```

---

### Task 10: 翻牌揭示页与解析页

**Files:**
- Create: `client/src/pages/RevealPage.tsx`, `client/src/pages/ReadingPage.tsx`, `client/src/components/FlipCard.tsx`, `client/src/components/InterpretationView.tsx`
- Modify: `client/src/App.tsx`(接入)

**Interfaces:**
- Consumes: `<CardFace/>`(Task 7)、`templateInterpret`(Task 6)、session(Task 8)
- Produces:
  - `<FlipCard drawn={DrawnCard} onFlipped? />`——3D 翻牌
  - 牌阵布局表 `SPREAD_LAYOUTS: Record<string, {x:number;y:number;rotate?:number}[]>`(x/y 为容器百分比)
  - `<InterpretationView data={Interpretation} />`
  - `<ReadingPage onAi?: () => void onSave?: () => void onRestart: () => void />`(AI/保存按钮的接线在 Task 12/13 完成,本任务按钮存在即可)

- [ ] **Step 1: FlipCard.tsx(3D 翻转)**

```tsx
import { motion } from "framer-motion";
import { useState } from "react";
import CardFace from "./CardFace";
import CardBack from "./CardBack";
import type { DrawnCard } from "../../../shared/core/types";

export default function FlipCard({ drawn, onFlipped }: { drawn: DrawnCard; onFlipped?: () => void }) {
  const [flipped, setFlipped] = useState(false);
  return (
    <div className="w-24 h-40 cursor-pointer" style={{ perspective: 800 }}
      onClick={() => { if (!flipped) { setFlipped(true); onFlipped?.(); } }}>
      <motion.div className="relative w-full h-full" style={{ transformStyle: "preserve-3d" }}
        animate={{ rotateY: flipped ? 180 : 0 }} transition={{ duration: 0.6 }}>
        <div className="absolute inset-0" style={{ backfaceVisibility: "hidden" }}><CardBack /></div>
        <div className="absolute inset-0" style={{ backfaceVisibility: "hidden", transform: "rotateY(180deg)" }}>
          <CardFace card={drawn.card} reversed={drawn.reversed} />
        </div>
      </motion.div>
    </div>
  );
}
```

- [ ] **Step 2: RevealPage.tsx(牌阵布局 + 逐张翻牌)**

```tsx
import { useState } from "react";
import FlipCard from "../components/FlipCard";
import { getSpread } from "../../../shared/core/spreads";
import { templateInterpret } from "../../../shared/core/interpret";
import type { Domain, DrawnCard, Interpretation } from "../../../shared/core/types";

// x/y:容器百分比;rotate:覆盖牌(凯尔特十字第 2 张横置)
export const SPREAD_LAYOUTS: Record<string, { x: number; y: number; rotate?: number }[]> = {
  "daily": [{ x: 50, y: 40 }],
  "time-flow": [{ x: 22, y: 40 }, { x: 50, y: 40 }, { x: 78, y: 40 }],
  "body-mind-spirit": [{ x: 50, y: 18 }, { x: 26, y: 58 }, { x: 74, y: 58 }],
  "diamond": [{ x: 50, y: 12 }, { x: 26, y: 45 }, { x: 74, y: 45 }, { x: 50, y: 78 }],
  "choice": [{ x: 50, y: 12 }, { x: 24, y: 38 }, { x: 76, y: 38 }, { x: 34, y: 70 }, { x: 66, y: 70 }],
  "grand-cross": [{ x: 22, y: 45 }, { x: 78, y: 45 }, { x: 50, y: 14 }, { x: 50, y: 76 }, { x: 50, y: 45 }],
  "celtic-cross": [
    { x: 36, y: 50 }, { x: 36, y: 50, rotate: 90 }, { x: 36, y: 80 }, { x: 14, y: 50 },
    { x: 36, y: 20 }, { x: 58, y: 50 }, { x: 84, y: 80 }, { x: 84, y: 60 },
    { x: 84, y: 40 }, { x: 84, y: 20 }
  ]
};

interface Props {
  spreadId: string; question: string; domain: Domain; drawn: DrawnCard[];
  onDone: (r: Interpretation) => void;
}

export default function RevealPage({ spreadId, question, domain, drawn, onDone }: Props) {
  const spread = getSpread(spreadId)!;
  const layout = SPREAD_LAYOUTS[spreadId];
  const [flippedCount, setFlippedCount] = useState(0);
  const allFlipped = flippedCount >= drawn.length;

  return (
    <div className="min-h-screen flex flex-col items-center px-6 py-10">
      <div className="relative w-full max-w-3xl h-[480px]">
        {drawn.map((d, i) => (
          <div key={i} className="absolute"
            style={{
              left: `${layout[i].x}%`, top: `${layout[i].y}%`,
              transform: `translate(-50%,-50%)${layout[i].rotate ? ` rotate(${layout[i].rotate}deg)` : ""}`
            }}>
            <FlipCard drawn={d} onFlipped={() => setFlippedCount(c => c + 1)} />
            <div className="text-center text-xs mt-1 opacity-60">{spread.positions[i].name}</div>
          </div>
        ))}
      </div>
      <p className="text-sm opacity-50 mt-2">{allFlipped ? "全部揭示" : "点击牌面逐张翻开"}</p>
      {allFlipped && (
        <button onClick={() => onDone(templateInterpret({ question, domain, spread, drawn }))}
          className="mt-6 px-10 py-3 border border-gold rounded-full text-goldlt tracking-[0.3em] hover:bg-gold/10">
          查看解读
        </button>
      )}
    </div>
  );
}
```

- [ ] **Step 3: InterpretationView.tsx 与 ReadingPage.tsx**

```tsx
import type { Interpretation } from "../../../shared/core/types";

export default function InterpretationView({ data }: { data: Interpretation }) {
  return (
    <div className="max-w-2xl mx-auto flex flex-col gap-6">
      <div className="border border-gold/40 rounded-xl p-6 bg-ink/60">
        <div className="text-goldlt tracking-widest mb-3">整体解读{data.source === "ai" ? " · AI 深度版" : ""}</div>
        <p className="leading-loose whitespace-pre-wrap">{data.summary}</p>
      </div>
      {data.positions.map((p, i) => (
        <div key={i} className="border border-gold/25 rounded-xl p-5 bg-ink/40">
          <div className="text-gold text-sm tracking-widest mb-2">
            {p.position.name} · {p.drawn.card.name}{p.drawn.reversed ? "(逆位)" : ""}
          </div>
          <p className="leading-loose text-sm opacity-90">{p.text}</p>
        </div>
      ))}
    </div>
  );
}
```

```tsx
import InterpretationView from "../components/InterpretationView";
import type { Interpretation } from "../../../shared/core/types";

interface Props {
  data: Interpretation;
  onAi?: () => void;
  onSave?: () => void;
  onRestart: () => void;
  aiLoading?: boolean;
  savedHint?: string;
}

export default function ReadingPage({ data, onAi, onSave, onRestart, aiLoading, savedHint }: Props) {
  return (
    <div className="min-h-screen px-6 py-12">
      <InterpretationView data={data} />
      <div className="flex justify-center gap-4 mt-10 flex-wrap">
        {onAi && (
          <button onClick={onAi} disabled={aiLoading}
            className="px-8 py-3 border border-goldlt rounded-full text-goldlt tracking-widest hover:bg-gold/10 disabled:opacity-40">
            {aiLoading ? "解读中…" : "✨ AI 深度解读"}
          </button>
        )}
        {onSave && (
          <button onClick={onSave}
            className="px-8 py-3 border border-gold/50 rounded-full tracking-widest hover:bg-gold/10">
            保存解读
          </button>
        )}
        <button onClick={onRestart}
          className="px-8 py-3 border border-gold/30 rounded-full tracking-widest opacity-70 hover:opacity-100">
          再占一次
        </button>
      </div>
      {savedHint && <p className="text-center text-xs opacity-50 mt-4">{savedHint}</p>}
    </div>
  );
}
```

- [ ] **Step 4: App.tsx 接入 reveal/reading**

```tsx
{s.phase === "reveal" && (
  <RevealPage spreadId={s.spreadId!} question={s.question} domain={s.domain} drawn={s.drawn}
    onDone={r => dispatch({ type: "interpreted", interpretation: r })} />
)}
{s.phase === "reading" && s.interpretation && (
  <ReadingPage data={s.interpretation} onRestart={() => dispatch({ type: "reset" })} />
)}
```

- [ ] **Step 5: 手动验收**

Run: `npm run dev:client`;完整流程:选凯尔特十字 → 抽 10 张 → 逐张翻牌(第 2 张横置覆盖)→ 查看解读 → 模板解读页展示逐位与整体叙事
Expected:翻转动画正常、逆位牌倒置显示、解读文案符合组合规则

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "feat(client): 翻牌揭示(3D 翻转/牌阵布局)与模板解读展示页"
```

---

### Task 11: 后端骨架——SQLite、口令鉴权与访客体系

**Files:**
- Create: `server/src/db.ts`, `server/src/app.ts`, `server/src/auth.ts`
- Modify: `server/src/index.ts`(改为使用 app.ts)
- Test: `server/src/__tests__/server.test.ts`

**Interfaces:**
- Produces:
  - `createDb(path?: string): Database`——建表 `visitors/readings/ai_cache/ai_usage`
  - `buildApp(opts: { db: Database; accessCode: string; aiCall?: AiCall }): Promise<FastifyInstance>`
  - `POST /api/auth`:body `{ code: string }` → 200 `{ token: string }` 或 401 `{ error: string }`
  - 鉴权约定:除 `/api/health` 与 `/api/auth` 外,所有 `/api/*` 需 `Authorization: Bearer <token>`;token 即 visitors 表 id
  - 供 Task 12 使用的 `request.visitorId: string`(由鉴权 preHandler 注入)

- [ ] **Step 1: 写失败测试**

`server/src/__tests__/server.test.ts`:
```ts
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { buildApp } from "../app";
import { createDb } from "../db";
import type { FastifyInstance } from "fastify";

let app: FastifyInstance;
beforeAll(async () => {
  app = await buildApp({ db: createDb(":memory:"), accessCode: "test-code" });
});
afterAll(() => app.close());

describe("鉴权", () => {
  it("健康检查无需鉴权", async () => {
    const r = await app.inject({ method: "GET", url: "/api/health" });
    expect(r.statusCode).toBe(200);
  });
  it("错误口令返回 401", async () => {
    const r = await app.inject({ method: "POST", url: "/api/auth", payload: { code: "wrong" } });
    expect(r.statusCode).toBe(401);
  });
  it("正确口令签发 token,且不同访客 token 不同", async () => {
    const r1 = await app.inject({ method: "POST", url: "/api/auth", payload: { code: "test-code" } });
    const r2 = await app.inject({ method: "POST", url: "/api/auth", payload: { code: "test-code" } });
    expect(r1.statusCode).toBe(200);
    const t1 = r1.json().token, t2 = r2.json().token;
    expect(t1).toBeTruthy();
    expect(t1).not.toBe(t2);
  });
  it("无 token 访问受保护接口返回 401", async () => {
    const r = await app.inject({ method: "GET", url: "/api/history" });
    expect(r.statusCode).toBe(401);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: FAIL(找不到模块 `../app`)

- [ ] **Step 3: 实现 db.ts / auth.ts / app.ts / index.ts**

`server/src/db.ts`:
```ts
import Database from "better-sqlite3";

export function createDb(path = process.env.DB_PATH ?? "data.db") {
  const db = new Database(path);
  db.exec(`
    CREATE TABLE IF NOT EXISTS visitors(
      id TEXT PRIMARY KEY,
      created_at TEXT DEFAULT (datetime('now'))
    );
    CREATE TABLE IF NOT EXISTS readings(
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      visitor_id TEXT NOT NULL,
      deck_id TEXT NOT NULL,
      question TEXT,
      spread_id TEXT NOT NULL,
      cards_json TEXT NOT NULL,
      interpretation_text TEXT NOT NULL,
      ai_enhanced INTEGER DEFAULT 0,
      created_at TEXT DEFAULT (datetime('now'))
    );
    CREATE TABLE IF NOT EXISTS ai_cache(
      key TEXT PRIMARY KEY,
      text TEXT NOT NULL,
      created_at TEXT DEFAULT (datetime('now'))
    );
    CREATE TABLE IF NOT EXISTS ai_usage(
      visitor_id TEXT NOT NULL,
      day TEXT NOT NULL,
      count INTEGER DEFAULT 0,
      PRIMARY KEY(visitor_id, day)
    );
  `);
  return db;
}
```

`server/src/auth.ts`:
```ts
import type { FastifyReply, FastifyRequest } from "fastify";
import type { Database } from "better-sqlite3";

declare module "fastify" {
  interface FastifyRequest { visitorId?: string; }
}

export function makeAuthHook(db: Database) {
  return async (req: FastifyRequest, reply: FastifyReply) => {
    const m = /^Bearer (.+)$/.exec(req.headers.authorization ?? "");
    const id = m?.[1];
    const ok = id && db.prepare("SELECT 1 FROM visitors WHERE id = ?").get(id);
    if (!ok) return reply.code(401).send({ error: "未授权" });
    req.visitorId = id;
  };
}
```

`server/src/app.ts`:
```ts
import Fastify from "fastify";
import { randomUUID } from "node:crypto";
import type { Database } from "better-sqlite3";
import { makeAuthHook } from "./auth";

export interface AppOptions {
  db: Database;
  accessCode: string;
  // Task 12 将扩展:aiCall?: (prompt: string) => Promise<string>
}

export async function buildApp(opts: AppOptions) {
  const { db, accessCode } = opts;
  const app = Fastify();

  app.get("/api/health", async () => ({ ok: true }));

  app.post("/api/auth", async (req, reply) => {
    const { code } = req.body as { code?: string };
    if (code !== accessCode) return reply.code(401).send({ error: "口令错误" });
    const token = randomUUID();
    db.prepare("INSERT INTO visitors(id) VALUES (?)").run(token);
    return { token };
  });

  const authHook = makeAuthHook(db);
  app.addHook("onRequest", async (req, reply) => {
    if (req.url.startsWith("/api/") && req.url !== "/api/auth" && req.url !== "/api/health") {
      await authHook(req, reply);
    }
  });

  // Task 12/13 在此注册 interpret 与 history 路由
  return app;
}
```

`server/src/index.ts` 改为:
```ts
import { buildApp } from "./app";
import { createDb } from "./db";

const port = Number(process.env.PORT ?? 8787);
const accessCode = process.env.ACCESS_CODE ?? "tarot";

buildApp({ db: createDb(), accessCode }).then(app => {
  app.listen({ port, host: "0.0.0.0" }).then(() => console.log(`server listening on ${port}`));
});
```

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: PASS 4/4

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "feat(server): SQLite 建表、口令鉴权与匿名访客 token 体系"
```

---

### Task 12: AI 解读代理(/api/interpret:缓存 + 限流)+ 前端接入

**Files:**
- Create: `server/src/ai.ts`, `server/src/interpret.ts`, `client/src/api/client.ts`, `client/src/components/PasscodeGate.tsx`
- Modify: `server/src/app.ts`(注册 interpret 路由)、`client/src/App.tsx`(加口令门)、`client/src/pages/ReadingPage.tsx` 调用处(App 内)
- Test: `server/src/__tests__/server.test.ts`(追加用例)

**Interfaces:**
- Consumes: `buildApp`(Task 11)、`AiCall`(Task 6)
- Produces:
  - `POST /api/interpret`(需鉴权),body:`{ question: string; domain: string; spreadName: string; items: { positionName: string; cardName: string; reversed: boolean }[] }`
    - 200 `{ text: string; cached: boolean }`;429 `{ fallback: true; reason: "daily-limit" }`;502 `{ fallback: true }`(AI 故障/未配置)
  - 缓存键:`sha256(JSON.stringify(body))`;限流:`ai_usage` 按 `visitor_id + date('now')` 计数,>= 20 拒绝
  - 前端 `requestAiReading(token, payload): Promise<string>`(10 秒 AbortController,非 200 抛错)
  - `<PasscodeGate onOk(token) />`;token 存 `localStorage["tarot_token"]`

- [ ] **Step 1: 追加失败测试**

在 `server/src/__tests__/server.test.ts` 追加:

```ts
describe("AI 解读代理", () => {
  async function freshApp(aiCall?: any) {
    return buildApp({ db: createDb(":memory:"), accessCode: "c", aiCall });
  }
  const body = {
    question: "q", domain: "general", spreadName: "每日一抽",
    items: [{ positionName: "今日指引", cardName: "愚人", reversed: false }]
  };
  async function tokenOf(app: any) {
    const r = await app.inject({ method: "POST", url: "/api/auth", payload: { code: "c" } });
    return r.json().token;
  }

  it("未配置 aiCall 时返回 502 fallback", async () => {
    const app = await freshApp();
    const token = await tokenOf(app);
    const r = await app.inject({ method: "POST", url: "/api/interpret", headers: { authorization: `Bearer ${token}` }, payload: body });
    expect(r.statusCode).toBe(502);
    expect(r.json().fallback).toBe(true);
    await app.close();
  });
  it("AI 成功返回文本,第二次相同请求命中缓存(不再调用 AI)", async () => {
    let calls = 0;
    const app = await freshApp(async () => { calls++; return "AI文本"; });
    const token = await tokenOf(app);
    const h = { authorization: `Bearer ${token}` };
    const r1 = await app.inject({ method: "POST", url: "/api/interpret", headers: h, payload: body });
    expect(r1.json()).toEqual({ text: "AI文本", cached: false });
    const r2 = await app.inject({ method: "POST", url: "/api/interpret", headers: h, payload: body });
    expect(r2.json()).toEqual({ text: "AI文本", cached: true });
    expect(calls).toBe(1);
    await app.close();
  });
  it("同一访客第 21 次请求被限流", async () => {
    const app = await freshApp(async () => "x");
    const token = await tokenOf(app);
    const h = { authorization: `Bearer ${token}` };
    for (let i = 0; i < 20; i++) {
      await app.inject({ method: "POST", url: "/api/interpret", headers: h, payload: { ...body, question: `q${i}` } });
    }
    const r = await app.inject({ method: "POST", url: "/api/interpret", headers: h, payload: { ...body, question: "q21" } });
    expect(r.statusCode).toBe(429);
    expect(r.json().reason).toBe("daily-limit");
    await app.close();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: 新增 3 个用例 FAIL(404),旧用例仍 PASS

- [ ] **Step 3: 实现 ai.ts 与 interpret.ts**

`server/src/ai.ts`:
```ts
export interface InterpretPayload {
  question: string;
  domain: string;
  spreadName: string;
  items: { positionName: string; cardName: string; reversed: boolean }[];
}

export function buildPrompt(p: InterpretPayload): string {
  const lines = p.items.map((it, i) =>
    `${i + 1}. 位置「${it.positionName}」:${it.cardName}(${it.reversed ? "逆位" : "正位"})`);
  return [
    "你是一位温和而专业的塔罗解读师。请根据以下占卜信息,用中文给出一段 200-350 字的整体解读:",
    "要求:把各位置的牌串联成一个连贯叙事;结合问题的领域侧重;语气温和、具体、不说教;不要逐张复述牌义,而是讲一个完整的故事;结尾给一句可执行的建议。",
    `问题:${p.question || "(未填写)"}(领域:${p.domain === "love" ? "爱情" : "综合"})`,
    `牌阵:${p.spreadName}`,
    ...lines
  ].join("\n");
}

export async function callAi(prompt: string): Promise<string> {
  const base = process.env.AI_BASE_URL;
  const key = process.env.AI_API_KEY;
  const model = process.env.AI_MODEL ?? "moonshot-v1-8k";
  if (!base || !key) throw new Error("AI 未配置");
  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), 10_000);
  try {
    const res = await fetch(`${base}/chat/completions`, {
      method: "POST",
      headers: { "content-type": "application/json", authorization: `Bearer ${key}` },
      body: JSON.stringify({ model, messages: [{ role: "user", content: prompt }], temperature: 0.8 }),
      signal: ctrl.signal
    });
    if (!res.ok) throw new Error(`AI HTTP ${res.status}`);
    const j = await res.json() as any;
    return j.choices[0].message.content as string;
  } finally {
    clearTimeout(timer);
  }
}
```

`server/src/interpret.ts`:
```ts
import type { FastifyInstance } from "fastify";
import { createHash } from "node:crypto";
import type { Database } from "better-sqlite3";
import { buildPrompt, type InterpretPayload } from "./ai";

const DAILY_LIMIT = 20;

export function registerInterpret(app: FastifyInstance, db: Database, aiCall?: (prompt: string) => Promise<string>) {
  app.post("/api/interpret", async (req, reply) => {
    const body = req.body as InterpretPayload;
    const visitorId = req.visitorId!;
    const day = new Date().toISOString().slice(0, 10);
    const usage = db.prepare("SELECT count FROM ai_usage WHERE visitor_id = ? AND day = ?")
      .get(visitorId, day) as { count: number } | undefined;
    if ((usage?.count ?? 0) >= DAILY_LIMIT) {
      return reply.code(429).send({ fallback: true, reason: "daily-limit" });
    }
    const key = createHash("sha256").update(JSON.stringify(body)).digest("hex");
    const hit = db.prepare("SELECT text FROM ai_cache WHERE key = ?").get(key) as { text: string } | undefined;
    if (hit) return { text: hit.text, cached: true };
    if (!aiCall) return reply.code(502).send({ fallback: true });
    try {
      const text = await aiCall(buildPrompt(body));
      db.prepare("INSERT OR REPLACE INTO ai_cache(key, text) VALUES (?, ?)").run(key, text);
      db.prepare(`INSERT INTO ai_usage(visitor_id, day, count) VALUES (?, ?, 1)
                  ON CONFLICT(visitor_id, day) DO UPDATE SET count = count + 1`).run(visitorId, day);
      return { text, cached: false };
    } catch {
      return reply.code(502).send({ fallback: true });
    }
  });
}
```

`app.ts` 中注册(在 addHook 之后):
```ts
import { registerInterpret } from "./interpret";
import { callAi } from "./ai";
// ...
const aiCall = opts.aiCall ?? (process.env.AI_API_KEY && process.env.AI_BASE_URL ? callAi : undefined);
registerInterpret(app, db, aiCall);
```
(`opts.aiCall` 类型相应改为 `(prompt: string) => Promise<string>`,测试里注入的假实现签名兼容。)

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: PASS 7/7

- [ ] **Step 5: 前端 api/client.ts 与 PasscodeGate.tsx**

`client/src/api/client.ts`:
```ts
export function getToken(): string | null {
  return localStorage.getItem("tarot_token");
}
export function setToken(t: string) {
  localStorage.setItem("tarot_token", t);
}

export async function auth(code: string): Promise<string> {
  const r = await fetch("/api/auth", {
    method: "POST", headers: { "content-type": "application/json" },
    body: JSON.stringify({ code })
  });
  if (!r.ok) throw new Error("口令错误");
  return (await r.json()).token as string;
}

export interface AiPayload {
  question: string; domain: string; spreadName: string;
  items: { positionName: string; cardName: string; reversed: boolean }[];
}

export async function requestAiReading(payload: AiPayload): Promise<string> {
  const ctrl = new AbortController();
  const timer = setTimeout(() => ctrl.abort(), 10_000);
  try {
    const r = await fetch("/api/interpret", {
      method: "POST",
      headers: { "content-type": "application/json", authorization: `Bearer ${getToken()}` },
      body: JSON.stringify(payload),
      signal: ctrl.signal
    });
    if (!r.ok) throw new Error(`HTTP ${r.status}`);
    return (await r.json()).text as string;
  } finally {
    clearTimeout(timer);
  }
}
```

`client/src/components/PasscodeGate.tsx`:
```tsx
import { useState } from "react";
import { auth, setToken } from "../api/client";

export default function PasscodeGate({ onOk }: { onOk: () => void }) {
  const [code, setCode] = useState("");
  const [err, setErr] = useState("");
  const submit = async () => {
    try { setToken(await auth(code)); onOk(); }
    catch { setErr("口令不对,再试试"); }
  };
  return (
    <div className="min-h-screen flex flex-col items-center justify-center gap-6 px-6">
      <div className="text-goldlt tracking-[0.4em] text-xl">星 轨 塔 罗</div>
      <input value={code} onChange={e => setCode(e.target.value)}
        onKeyDown={e => e.key === "Enter" && submit()}
        type="password" placeholder="请输入访问口令"
        className="w-64 bg-ink/60 border border-gold/40 rounded-xl px-4 py-3 text-center text-goldlt placeholder:opacity-40 focus:border-gold outline-none" />
      {err && <div className="text-red-400 text-sm">{err}</div>}
      <button onClick={submit}
        className="px-10 py-3 border border-gold rounded-full text-goldlt tracking-[0.3em] hover:bg-gold/10">
        进 入
      </button>
    </div>
  );
}
```

`App.tsx` 顶部加门:
```tsx
const [authed, setAuthed] = useState(Boolean(getToken()));
if (!authed) return <PasscodeGate onOk={() => setAuthed(true)} />;
```
(同时 `import { getToken } from "./api/client";`、`import PasscodeGate from "./components/PasscodeGate";`、`import { useState } from "react";`)

- [ ] **Step 6: App.tsx 给 ReadingPage 接上 AI 按钮**

```tsx
{s.phase === "reading" && s.interpretation && (
  <ReadingPage data={s.interpretation}
    aiLoading={aiLoading}
    onAi={async () => {
      setAiLoading(true);
      const spread = getSpread(s.spreadId!)!;
      const payload = {
        question: s.question, domain: s.domain, spreadName: spread.name,
        items: s.drawn.map((d, i) => ({
          positionName: spread.positions[i].name,
          cardName: d.card.name, reversed: d.reversed
        }))
      };
      const r = await interpretWithFallback(
        { question: s.question, domain: s.domain, spread, drawn: s.drawn },
        () => requestAiReading(payload)
      );
      dispatch({ type: "interpreted", interpretation: r });
      setAiLoading(false);
    }}
    onRestart={() => dispatch({ type: "reset" })} />
)}
```
(`import { interpretWithFallback } from "../../shared/core/interpret";` 等相应导入;`aiLoading` 用 `useState`。)

- [ ] **Step 7: 手动验收**

Run: `npm run dev`(前后端同起);无 `.env` 时点"✨ AI 深度解读"
Expected: 按钮转"解读中…"后回退为基础解读,页面仍正常;配置 `.env`(复制 `.env.example` 填真 Key)后可得 AI 文本(Task 14 提供 `.env.example`)

- [ ] **Step 8: Commit**

```bash
git add -A && git commit -m "feat: AI 解读代理(缓存+每日限流 20)与前端口令门/AI 按钮接入"
```

---

### Task 13: 解读记录(/api/history)与历史页

**Files:**
- Create: `server/src/history.ts`
- Modify: `server/src/app.ts`(注册路由)、`client/src/api/client.ts`(加 history 方法)、`client/src/pages/HistoryPage.tsx`(新建)、`client/src/App.tsx`(保存按钮与历史页接入)
- Test: `server/src/__tests__/server.test.ts`(追加用例)

**Interfaces:**
- Produces:
  - `POST /api/history`(需鉴权)body `{ question, deckId, spreadId, cards, interpretationText, aiEnhanced }` → 200 `{ id: number }`
  - `GET /api/history`(需鉴权)→ `{ items: { id, question, spreadId, aiEnhanced, createdAt }[] }`(倒序,只含本人的)
  - 前端 `saveReading(payload): Promise<number>`、`listReadings(): Promise<HistoryItem[]>`

- [ ] **Step 1: 追加失败测试**

```ts
describe("解读记录", () => {
  it("保存并按访客隔离读取", async () => {
    const app = await buildApp({ db: createDb(":memory:"), accessCode: "c" });
    const t = async () => (await app.inject({ method: "POST", url: "/api/auth", payload: { code: "c" } })).json().token;
    const tA = await t(), tB = await t();
    const rec = { question: "q", deckId: "rws", spreadId: "daily", cards: [{ id: "major-00", reversed: false, position: 0 }], interpretationText: "解读", aiEnhanced: false };
    const r1 = await app.inject({ method: "POST", url: "/api/history", headers: { authorization: `Bearer ${tA}` }, payload: rec });
    expect(r1.statusCode).toBe(200);
    const listA = (await app.inject({ method: "GET", url: "/api/history", headers: { authorization: `Bearer ${tA}` } })).json();
    const listB = (await app.inject({ method: "GET", url: "/api/history", headers: { authorization: `Bearer ${tB}` } })).json();
    expect(listA.items).toHaveLength(1);
    expect(listA.items[0].question).toBe("q");
    expect(listB.items).toHaveLength(0);
    await app.close();
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: 新用例 FAIL(404)

- [ ] **Step 3: 实现 history.ts 并注册**

`server/src/history.ts`:
```ts
import type { FastifyInstance } from "fastify";
import type { Database } from "better-sqlite3";

interface SaveBody {
  question: string; deckId: string; spreadId: string;
  cards: unknown; interpretationText: string; aiEnhanced: boolean;
}

export function registerHistory(app: FastifyInstance, db: Database) {
  app.post("/api/history", async (req) => {
    const b = req.body as SaveBody;
    const r = db.prepare(`INSERT INTO readings(visitor_id, deck_id, question, spread_id, cards_json, interpretation_text, ai_enhanced)
                          VALUES (?, ?, ?, ?, ?, ?, ?)`)
      .run(req.visitorId, b.deckId, b.question ?? "", b.spreadId,
           JSON.stringify(b.cards), b.interpretationText, b.aiEnhanced ? 1 : 0);
    return { id: Number(r.lastInsertRowid) };
  });

  app.get("/api/history", async (req) => {
    const rows = db.prepare(`SELECT id, question, spread_id AS spreadId, ai_enhanced AS aiEnhanced, created_at AS createdAt
                             FROM readings WHERE visitor_id = ? ORDER BY id DESC LIMIT 100`)
      .all(req.visitorId);
    return { items: rows };
  });
}
```

`app.ts`:`import { registerHistory } from "./history";` + `registerHistory(app, db);`

- [ ] **Step 4: 运行确认通过**

Run: `npx vitest run server/src/__tests__/server.test.ts`
Expected: PASS 8/8

- [ ] **Step 5: 前端接入**

`client/src/api/client.ts` 追加:
```ts
export interface HistoryItem { id: number; question: string; spreadId: string; aiEnhanced: number; createdAt: string; }

export async function saveReading(payload: {
  question: string; deckId: string; spreadId: string;
  cards: unknown; interpretationText: string; aiEnhanced: boolean;
}): Promise<number> {
  const r = await fetch("/api/history", {
    method: "POST",
    headers: { "content-type": "application/json", authorization: `Bearer ${getToken()}` },
    body: JSON.stringify(payload)
  });
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  return (await r.json()).id as number;
}

export async function listReadings(): Promise<HistoryItem[]> {
  const r = await fetch("/api/history", { headers: { authorization: `Bearer ${getToken()}` } });
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  return (await r.json()).items as HistoryItem[];
}
```

`client/src/pages/HistoryPage.tsx`:
```tsx
import { useEffect, useState } from "react";
import { listReadings, type HistoryItem } from "../api/client";

export default function HistoryPage({ onBack }: { onBack: () => void }) {
  const [items, setItems] = useState<HistoryItem[]>([]);
  const [err, setErr] = useState("");
  useEffect(() => { listReadings().then(setItems).catch(() => setErr("暂时无法读取历史")); }, []);
  return (
    <div className="min-h-screen px-6 py-12 max-w-2xl mx-auto">
      <button onClick={onBack} className="text-sm opacity-60 hover:opacity-100 mb-8">← 返回</button>
      <h2 className="text-goldlt tracking-[0.4em] mb-8">过往解读</h2>
      {err && <p className="opacity-50">{err}</p>}
      {!err && items.length === 0 && <p className="opacity-50">还没有保存过解读</p>}
      <div className="flex flex-col gap-4">
        {items.map(it => (
          <div key={it.id} className="border border-gold/25 rounded-xl p-4 bg-ink/40">
            <div className="text-goldlt">{it.question || "(未填写问题)"}</div>
            <div className="text-xs opacity-50 mt-2">
              {it.createdAt} · {it.spreadId}{it.aiEnhanced ? " · AI 深度版" : ""}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

`App.tsx`:history 分支渲染 `<HistoryPage onBack={() => dispatch({type:"reset"})} />`;ReadingPage 的 `onSave`:
```tsx
onSave={async () => {
  const text = [s.interpretation!.summary,
    ...s.interpretation!.positions.map(p => p.text)].join("\n\n");
  await saveReading({
    question: s.question, deckId: s.deckId, spreadId: s.spreadId!,
    cards: s.drawn.map((d, i) => ({ id: d.card.id, reversed: d.reversed, position: i })),
    interpretationText: text, aiEnhanced: s.interpretation!.source === "ai"
  });
  setSavedHint("已保存到历史");
}}
```
(`savedHint` state 传给 ReadingPage 的 `savedHint`。)

- [ ] **Step 6: 手动验收**

Run: `npm run dev`;完成一次占卜 → 保存 → 首页进历史页可见记录;换浏览器(新访客)历史为空
Expected: 记录按访客隔离

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat: 解读记录保存与历史页(按访客隔离)"
```

---

### Task 14: 生产部署(静态伺服 / .env / pm2 / 部署脚本 / 文档)

**Files:**
- Modify: `server/src/index.ts`(生产模式伺服 `dist/`)
- Create: `.env.example`, `ecosystem.config.js`, `deploy.sh`, `README.md`

**Interfaces:**
- Produces:Linux 服务器上 `git clone → npm install → npm run build → pm2 start ecosystem.config.js` 即可运行

- [ ] **Step 1: index.ts 生产静态伺服与 .env 加载**

先安装:`npm install @fastify/static dotenv`

```ts
import "dotenv/config";
import { buildApp } from "./app";
import { createDb } from "./db";
import fastifyStatic from "@fastify/static";
import { fileURLToPath } from "node:url";
import { dirname, join } from "node:path";

const port = Number(process.env.PORT ?? 8787);
const accessCode = process.env.ACCESS_CODE ?? "tarot";

buildApp({ db: createDb(), accessCode }).then(async app => {
  if (process.env.NODE_ENV === "production") {
    const root = join(dirname(fileURLToPath(import.meta.url)), "../../dist");
    await app.register(fastifyStatic, { root });
    app.setNotFoundHandler((_req, reply) => reply.sendFile("index.html")); // SPA 回退
  }
  await app.listen({ port, host: "0.0.0.0" });
  console.log(`server listening on ${port}`);
});
```

- [ ] **Step 2: .env.example**

```
ACCESS_CODE=change-me
AI_BASE_URL=https://api.moonshot.cn/v1
AI_API_KEY=
AI_MODEL=moonshot-v1-8k
PORT=8787
DB_PATH=data.db
```

- [ ] **Step 3: ecosystem.config.js 与 deploy.sh**

`ecosystem.config.js`:
```js
module.exports = {
  apps: [{
    name: "tarot",
    script: "node_modules/.bin/tsx",
    args: "server/src/index.ts",
    env: { NODE_ENV: "production" }
  }]
};
```

`deploy.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
git pull
npm install
npm run build
pm2 reload ecosystem.config.js || pm2 start ecosystem.config.js
echo "deployed at $(date)"
```

- [ ] **Step 4: README.md(运行与部署说明)**

写明:本地开发 `npm install && npm run dev`(前端 7100,后端 8787);测试 `npm test`;构建 `npm run build`;生产部署四步;`.env` 各字段含义;nginx + HTTPS 为可选增强。

- [ ] **Step 5: 验证**

Run: `npm run build && NODE_ENV=production npm start`
Expected: 构建产物在 `dist/`;`curl http://localhost:8787/` 返回 HTML;`curl http://localhost:8787/api/health` 返回 `{"ok":true}`;验证后 Ctrl+C

- [ ] **Step 6: Commit**

```bash
git add -A && git commit -m "chore: 生产部署——静态伺服/pm2/部署脚本/文档"
```

---

### Task 15(第二阶段):通用 Deck 模型落地 + 月相神谕卡数据包 + 牌组选择

**Files:**
- Create: `shared/data/cards.moonology.json`
- Modify: `client/src/pages/HomePage.tsx`(加牌组切换)、`client/src/pages/DrawPage.tsx`(按 deckId 选数据)、`client/src/App.tsx`
- Test: `shared/core/__tests__/cards.test.ts`(追加神谕卡校验)

**Interfaces:**
- Consumes:全部既有接口(Task 2 的类型已为 oracle 设计好)
- Produces:`DECKS: Record<string, DeckData>`(`client/src/decks.ts` 导出,首页与抽牌页共用)

**数据编写规则:**
1. 来源:`D:\BaiduNetdiskDownload\穆夏塔罗-翻译\Y\月相\Moonology Oracle月亮学神谕卡.pdf`(有文本层,先 `pypdf` 提取全文到 `tarot-references/moonology.txt`,再逐卡改写)
2. 结构:`{"id":"moonology","name":"月相神谕卡","type":"oracle","cards":[...]}`;每卡 `arcana:"oracle"`、`suit:null`、`reversed:null`、`number` 从 1 递增
3. `upright` = 该卡的指引句(改写,40–80 字);`domains.general` 同 upright 或扩写;`symbols` 2–3 个(moon/sun/star/sparkle 等)

- [ ] **Step 1: 提取月相原文到工作区**

Run:
```bash
cd "D:\KimiData\kimi\workspace\tarot-references"
python -c "
from pypdf import PdfReader
import shutil
shutil.copy(r'D:\BaiduNetdiskDownload\穆夏塔罗-翻译\Y\月相\Moonology Oracle月亮学神谕卡.pdf', 'moonology.pdf')
r = PdfReader('moonology.pdf')
open('moonology.txt','w',encoding='utf-8').write('\n'.join((p.extract_text() or '') for p in r.pages))
print('extracted', len(r.pages), 'pages')
"
```
Expected: 生成 `moonology.txt`

- [ ] **Step 2: 追加校验测试**

`cards.test.ts` 追加:
```ts
import moonology from "../../data/cards.moonology.json";

describe("月相神谕卡数据包", () => {
  it("不少于 30 张,全部无逆位", () => {
    expect(moonology.cards.length).toBeGreaterThanOrEqual(30);
    for (const c of moonology.cards) {
      expect(c.arcana).toBe("oracle");
      expect(c.reversed).toBeNull();
    }
  });
});
```

- [ ] **Step 3: 运行确认失败**

Run: `npx vitest run shared/core/__tests__/cards.test.ts`
Expected: FAIL(`cards.moonology.json` 不存在)

- [ ] **Step 4: 编写月相数据包**

按规则写全整套(以原文张数为准),运行校验至 PASS。

- [ ] **Step 5: 牌组切换 UI**

`client/src/decks.ts`:
```ts
import type { DeckData } from "../../shared/core/types";
import tarot from "../../shared/data/cards.tarot.json";
import moonology from "../../shared/data/cards.moonology.json";
export const DECKS: Record<string, DeckData> = {
  rws: tarot as unknown as DeckData,
  moonology: moonology as unknown as DeckData
};
```

HomePage 顶部加牌组切换(塔罗 | 月相神谕卡),选中神谕卡时 SpreadPicker 只显示 `daily`(神谕卡复用每日一抽);DrawPage 按 `deckId` 从 `DECKS` 取数据给 `createDeck`。App 的 `select-spread` action 不变,`reset` 保留 deckId。

- [ ] **Step 6: 运行全量测试 + 手动验收**

Run: `npm test`(Expected: 全部 PASS)与 `npm run dev` 手动走一遍神谕卡每日一抽流程
Expected: 神谕卡抽牌无逆位、解读正常、历史记录 deckId 正确

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "feat: 第二阶段——通用 Deck 模型落地与月相神谕卡数据包"
```

---

## 完成标准(Definition of Done)

- `npm test` 全绿(core 20+ 用例 + server 8 用例)
- `npm run dev` 本地全流程可走通(口令 → 选牌阵 → 抽牌 → 翻牌 → 模板解读 → AI 解读 → 保存 → 历史)
- `npm run build && NODE_ENV=production npm start` 产物可由后端伺服
- 牌面视觉与 `tarot-preview/preview.png` 基准一致
- 部署脚本在 Linux 服务器实测通过(owner 配合执行)
